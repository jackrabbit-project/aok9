node_modules/
*.local
.DS_Store

# Build artifacts
tsconfig.tsbuildinfo

# Downloaded copies of the published Grading Guide (npm run refresh-guide).
# The converted JSON in src/data is what ships; the xlsx is just the input.
.guide-cache/

# Local editor/tooling config
.claude/
